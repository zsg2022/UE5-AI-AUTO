# 代码审查报告 — UE5AIAUTO 插件全量

## 审查范围

CppTools.cpp (398) + EditorSubsystem.cpp (454) + AdvancedTools.cpp (181) + BlueprintEditor.cpp (60) + WebSocketClient.cpp (前70) ≈ **1200 行**

## 总体评估

代码整体健康，功能齐全、错误处理统一、风格紧凑一致。主要问题是 **路径加载不一致**（新旧混合）、**UE5.7 弃用 API 残留**、**缩进混用**。无 🔴 阻断项，5 🟡 可修复，4 🟢 建议改进。

## 发现清单

| # | 级别 | 维度 | 位置 | 问题 | 建议 |
|---|:---:|------|------|------|------|
| 1 | 🟡 | 风格+F4 | `EditorSubsystem.cpp:390-395` | 新增 widget handler 的缩进（tab×3）与周围代码（1 tab）不一致，且缩进偏移可能导致阅读者误解作用域 | 对齐到 1 个 tab，与 `add_widget_to_canvas` 等老代码一致 |
| 2 | 🟡 | 可读+D5 | `CppTools.cpp:234` | `AddWidgetToCanvas` 用 `LoadObject<UWidgetBlueprint>(nullptr,*Path)` 原始路径，但 `_LoadWBP` helper(line 261) 已封装了正确的点号路径回退。不一致——同一个文件里新函数用 `_LoadWBP`，老函数不用 | `AddWidgetToCanvas` 改用 `_LoadWBP(Path)` |
| 3 | 🟡 | 性能+F2 | `CppTools.cpp:267` | `Btn->GetChildAt(0)` 假设 Button 第一个子节点是 TextBlock。如果用户先加了 Image 或 Border 到 Button 里，这里会错误地把 TextBlock 的 text 设给一个 Image | 遍历子节点找 `UTextBlock`，而非假设 index 0 |
| 4 | 🟡 | 功能+F4 | `CppTools.cpp:306-340` | `AddAnimState`、`AddAnimTransition`、`SetAnimGraphNode` 用 `LoadObject<UAnimBlueprint>(nullptr,*Path)` 未做点号路径转换，和 BT/BB/DataTable/WBP 不一致 | 统一为点号路径回退模式 |
| 5 | 🟡 | 异常+F4 | `CppTools.cpp:375` | `FJsonObjectConverter::JsonObjectToUStruct` 返回 `bool` 表示成功/失败，但返回值未检查。如果 JSON 字段类型不匹配 RowStruct，会静默失败，行数据被写入默认值 | 检查返回值，返回错误信息 |
| 6 | 🟢 | 可读 | `EditorSubsystem.cpp:390-395` | 新 widget handler 的参数 `w`/`h` 在 server.py 侧叫 `w`/`h`，但 `WidgetSetPosition` 已改名为 `Width`/`Height`。C++ handler 侧仍用 `w`/`h` 作 JSON 键名 | 统一为 `w`/`h` 或 `width`/`height`，选一端对齐 |
| 7 | 🟢 | 性能 | `CppTools.cpp:160,177,192` | `AddBTTask/AddBTDecorator/AddBTService` 用 `TObjectIterator<UClass>` 全局遍历查找类名，每次调用都遍历所有已加载类 | 首次查找后缓存到 `static TMap<FString,UClass*>` |
| 8 | 🟢 | 安全+F4 | `WebSocketClient.cpp:129` | `ReadBuffer.AppendChars` 将 TCP 接收的字节直接按 UTF-8 转 TCHAR。如果 JSON 消息在 64KB buffer 边界被截断，多字节 UTF-8 字符会被损坏 | 在产生完整 JSON 行（`\n` 结束）后再做 UTF-8 转换 |
| 9 | 🟢 | 安全 | `EditorSubsystem.cpp:301-302` | `BP_REG` 宏注释说明"只支持同步路径"，但无编译期保护。若被意外用于异步队列，lambda 引用的 `P` 会悬空 | 可接受——注释已足够警示 |

## 审查统计

| 维度 | 🔴 阻断 | 🟡 重要 | 🟢 建议 | ⚪ 观察 |
|------|:---:|:---:|:---:|:---:|
| 架构设计 | 0 | 1 | 0 | 0 |
| 功能正确性 | 0 | 2 | 0 | 0 |
| 安全编码 | 0 | 0 | 2 | 0 |
| 性能 | 0 | 0 | 2 | 0 |
| 异常处理 | 0 | 1 | 0 | 0 |
| 可读性/风格 | 0 | 1 | 1 | 0 |
| **合计** | **0** | **5** | **5** | **0** |

## 合并建议

🟡 全部 5 项可批量修复（预计 30 分钟），建议优先 #2（路径一致性）和 #5（静默失败），其余可延后。
